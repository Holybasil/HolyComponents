import HolyComponents from './../../src/main'
import "../../dist/holycomponents.css"

export default ({ Vue, options, router, siteData }) => {
  Vue.use(HolyComponents)
  // console.log(HolyComponents, 'eemmmm');
}